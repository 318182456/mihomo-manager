{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // Worker 运行在 CF edge，不使用浏览器 DOM API
    "lib": ["ES2022"],
    "types": ["@cloudflare/workers-types"],
    "noEmit": true
  },
  "include": ["index.ts"]
}
